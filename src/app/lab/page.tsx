// Phase 3: card design lab (dev only — DESIGN_GUIDE §6 matrix)
import type { Metadata } from 'next'

export const metadata: Metadata = {
  robots: { index: false, follow: false },
}

export default function LabPage() {
  return <main />
}
